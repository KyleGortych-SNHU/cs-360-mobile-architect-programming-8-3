<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Runtime permission is also requested at use-time in SmsActivity. -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- android:theme references the library-provided Material 3 theme so you
         don't need any custom themes.xml.  Android Studio's "No Activity"
         template already includes the Material dependency. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- LoginActivity is the entry point so users always authenticate
             before they can reach the inventory grid. -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DataActivity"
            android:exported="false" />

        <activity
            android:name=".SmsActivity"
            android:exported="false" />

    </application>

</manifest>